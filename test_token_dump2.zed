aktar "lexer.zed";
degisken kod = "eger x > n {";
degisken tokenlar = tara(kod);
degisken i = 0;
iken i < uzunluk(tokenlar) {
    yaz metine(i) + ": " + tokenlar[i][0] + " = '" + tokenlar[i][1] + "'";
    i = i + 1;
}
